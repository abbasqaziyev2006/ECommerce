@model List<ProductViewModel>

@foreach (var product in Model)
{
    <div class="col-md-4 my-4">
        <div class="card position-relative">
            <a asp-controller="Product" asp-action="Details" asp-route-id="@product.Id">
                <img src="images/@product.CoverImageName" class="img-fluid rounded-4" alt="image">
            </a>
            <div class="card-body p-0">
                <a asp-controller="Product" asp-action="Details" asp-route-id="@product.Id">
                    <h3 class="card-title pt-4 m-0">@product.Name</h3>
                </a>

                <div class="card-text">
                    <span class="rating secondary-font">
                        @for (int i = 0; i < 5; i++)
                        {
                            <iconify-icon icon="clarity:star-solid" class="text-primary"></iconify-icon>
                        }
                        5.0
                    </span>

                    <h3 class="secondary-font text-primary">
                        $@product.Price.ToString("0.#")
                    </h3>

                    <div class="d-flex flex-wrap mt-3">
                        <a onclick="addToBasket(@product.Id)" class="btn-cart me-3 px-4 pt-3 pb-3" data-product-id="@product.Id">
                            <h5 class="text-uppercase m-0">Add to Cart</h5>
                        </a>
                        <a onclick="addToWishlist(@product.Id)"
                           class="btn-wishlist px-4 pt-3 btn btn-outline-primary add-to-wishlist"
                           data-id="@product.Id">
                            <iconify-icon icon="fluent:heart-28-filled" class="fs-5"></iconify-icon>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
}